name: Deploy Development

on:
  push:
    branches:
      - staging

concurrency:
  group: deploy-development
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy development
    if: ${{ !contains(github.event.head_commit.message || '', '[skip release]') }}
    runs-on: blacksmith-2vcpu-ubuntu-2404
    environment: development
    env:
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      RAILWAY_PROJECT_ID: ${{ vars.RAILWAY_PROJECT_ID }}
      RAILWAY_SERVICE_NAME: ${{ vars.RAILWAY_SERVICE_NAME || 'web' }}
      DEVELOPMENT_APP_URL: ${{ vars.DEVELOPMENT_APP_URL }}
      CONVEX_DEPLOY_KEY: ${{ secrets.CONVEX_DEPLOY_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.11

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Lint
        run: bun run lint

      - name: Test
        run: bun test

      - name: Build
        run: bun run build

      - name: Deploy Convex
        run: npx convex deploy

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy Railway development
        run: >
          railway up
          --project "$RAILWAY_PROJECT_ID"
          --environment development
          --service "$RAILWAY_SERVICE_NAME"
          --ci
          -m "development ${GITHUB_SHA}"

      - name: Smoke test
        run: curl --fail --show-error --silent "${DEVELOPMENT_APP_URL}/api/healthz"
